<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Comuvicdam_tf_202526Dvargas">
        <activity
            android:name=".activity.AddProfesionActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ReservationConfirmationActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ReservationActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ProfesionDetailActivity"
            android:exported="false" />
        <activity
            android:name=".activity.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".activity.LoginActivity"
            android:exported="false" /> <!-- MainActivity: pantalla normal (más adelante será el catálogo) -->
        <activity
            android:name=".activity.MainActivity"
            android:exported="false" /> <!-- WelcomeActivity: pantalla de bienvenida -->
        <activity
            android:name=".activity.WelcomeActivity"
            android:exported="false" /> <!-- SplashActivity: ESTA es la que arranca la app -->
        <activity
            android:name=".activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>