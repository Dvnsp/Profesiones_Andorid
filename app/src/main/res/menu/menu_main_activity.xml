<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Icono de carrito SIEMPRE visible -->
    <item
        android:id="@+id/action_cart"
        android:title="@string/menu_action_cart"
        android:icon="@drawable/baseline_shopping_cart_24"
        app:showAsAction="always" />

    <!-- Escanear QR (en los tres puntos) -->
    <item
        android:id="@+id/action_scan_qr"
        android:title="@string/menu_action_scan_qr"
        app:showAsAction="never" />

    <!-- Ayuda -->
    <item
        android:id="@+id/action_help"
        android:title="@string/menu_action_help"
        app:showAsAction="never" />

    <!-- Acerca de -->
    <item
        android:id="@+id/action_about"
        android:title="@string/menu_action_about"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_logout"
        android:title="@string/menu_action_logout"
        android:icon="@android:drawable/ic_lock_power_off"
        android:showAsAction="never" />
</menu>
